@model IEnumerable<ERMS.ViewModels.Department.DepartmentViewModel>
@{
    ViewData["Title"] = "Departments";
}

<link rel="stylesheet" href="~/css/Department-Position.css" />

<div class="pp-wrap">

    <!-- Header -->
    <div class="pp-header">
        <div>
            <p class="pp-eyebrow">Administration</p>
            <h1 class="pp-title">Departments</h1>
        </div>
        <a asp-action="Create" class="btn btn-primary">
            <span class="glyphicon glyphicon-plus" style="font-size:12px;"></span>
            New Department
        </a>
    </div>

    <!-- Toast alerts -->
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="pp-alert success">
            <span class="pp-alert-icon glyphicon glyphicon-ok"></span>
            <div class="pp-alert-content">@TempData["SuccessMessage"]</div>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="pp-alert danger">
            <span class="pp-alert-icon glyphicon glyphicon-exclamation-sign"></span>
            <div class="pp-alert-content">@TempData["ErrorMessage"]</div>
        </div>
    }

    <!-- Table card -->
    <div class="pp-card">
        <div class="pp-card-head">
            <span class="pp-card-label">
                <span class="pp-card-icon"><span class="glyphicon glyphicon-th-large"></span></span>
                All Departments
            </span>
            <span class="pp-chip pp-chip-accent">
                @Model.Count() department@(Model.Count() == 1 ? "" : "s")
            </span>
        </div>

        @if (Model.Any())
        {
            <div class="pp-table-wrap">
                <table class="pp-table">
                    <thead>
                        <tr>
                            <th>Department</th>
                            <th class="right">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <div class="pp-table-title">@item.Name</div>
                                    @if (!string.IsNullOrWhiteSpace(item.Description))
                                    {
                                        <div class="pp-table-sub">@item.Description</div>
                                    }
                                </td>
                                <td class="right">
                                    <div style="display:inline-flex; gap:6px;">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">
                                            <span class="glyphicon glyphicon-pencil"></span>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">
                                            <span class="glyphicon glyphicon-trash"></span>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="pp-empty">
                <div class="pp-empty-icon">
                    <span class="glyphicon glyphicon-th-large"></span>
                </div>
                <div class="pp-empty-title">No departments yet</div>
                <div class="pp-empty-sub">Create your first department to start organising employees.</div>
                <a asp-action="Create" class="btn btn-primary" style="margin-top:8px;">
                    <span class="glyphicon glyphicon-plus" style="font-size:12px;"></span>
                    New Department
                </a>
            </div>
        }
    </div>

</div>
